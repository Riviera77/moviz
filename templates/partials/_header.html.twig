<nav class="navbar navbar-expand-lg navbar-dark bg-dark border-bottom shadow-sm">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center me-3" href="{{ path('app_home') }}">
      <img src="{{ asset('build/images/logo-moviz.png') }}" alt="Logo Moviz" height="35" class="me-2 img-fluid" style="max-height: 80px;">
    </a>

    <!-- Bouton burger -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar"
      aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Contenu du menu -->
    <div class="collapse navbar-collapse justify-content-between" id="mainNavbar">
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link text-white {% if app.request.get('_route') == 'app_home' %}active{% endif %}" href="{{ path('app_home') }}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white {% if app.request.get('_route') == 'app_movie' %}active{% endif %}" href="{{ path('app_movie') }}">Les films</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white {% if app.request.get('_route') == 'app_about' %}active{% endif %}" href="{{ path('app_about') }}">À propos</a>
        </li>
        {% if is_granted('ROLE_ADMIN') %}
          <li class="nav-item">
            <a class="nav-link text-uppercase fw-bold text-warning" href="{{ path('admin') }}">Admin</a>
          </li>
        {% endif %}
      </ul>

      <!-- Boutons à droite -->
      <div class="d-flex align-items-center ms-lg-3">
        {% if app.user %}
          <span class="me-2 text-white small">👋 Bonjour {{ app.user.nickname ?? app.user.email }}</span>
          <a href="{{ path('app_logout') }}" class="btn btn-outline-danger btn-sm">Logout</a>
        {% else %}
          <a href="{{ path('app_login') }}" class="btn btn-outline-light btn-sm me-2">Connexion</a>
          <a href="{{ path('app_register') }}" class="btn btn-primary btn-sm">S'inscrire</a>
        {% endif %}
      </div>
    </div>
  </div>
</nav>
